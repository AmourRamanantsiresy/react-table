What is this?
This is the way I found to transform the tables in html format into json format 

It's the script.js script that does it


the HTML data is in the file data.txt and the result is in result.json
